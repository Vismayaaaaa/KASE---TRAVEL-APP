```
}, []);

const          borderBottom: isScrolled ? '1px solid rgba(0,0,0,0.05)' : 'none',
          padding: isScrolled ? '16px 0' : '24px 0 0 0'
        }}
      >
        <div className="container">
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: isScrolled ? 0 : '16px' }}>
            {/* Logo - New Brand */}
            <Link to="/" style={{ display: 'flex', alignItems: 'center', gap: '10px', flex: 1, textDecoration: 'none' }}> e.currentTarget.style.boxShadow = 'var(--shadow-md)';
  e.currentTarget.style.transform = 'translateY(-1px)';
}}
);

export default Navbar;
```
